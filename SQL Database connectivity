/*The below C code segment shows how to connect to an existing SQL database. If the database does not exist, then it will be created and finally a database object will be returned. The dataase is used for storing details of the user and WPM (words per minute) to calculate the user's typing speed and assess using a standardised metric*/

#include <stdio.h>
#include <sqlite3.h>
int main(int argc, char* argv[]) {
   sqlite3 *db;
   char *zErrMsg = 0;
   int rc;
   rc = sqlite3_open("test.db", &db);
   if( rc ) {
      fprintf(stderr, "Can't open database: %s\n", sqlite3_errmsg(db));
      return(0);
   } else {
      fprintf(stderr, "Opened database successfully\n");
   }
   sqlite3_close(db);
}
